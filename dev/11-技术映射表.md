# 跨平台技术映射表

**版本**: 2.0.0  
**更新日期**: 2026-01-11  
**文档类型**: 📋 技术映射参考（Windows/macOS/Linux 统一方案）
**状态**: 参考
**适用范围**: 跨平台技术映射对照
**维护人**: zy


---

## 一、核心技术栈

| 组件 | 技术方案 | 说明 |
|------|---------|------|
| 运行时 | Node.js 20+ | 跨平台 JavaScript 运行时 |
| 语言 | TypeScript 5.x | 类型安全 |
| 包管理 | pnpm | 高效、workspace 支持 |
| GUI 框架 | Electron | 跨平台桌面应用 |
| 测试框架 | Vitest | 快速、TypeScript 原生 |

---

## 二、系统控制 npm 包映射

| 功能 | npm 包 | Windows | macOS | Linux | 备选方案 |
|------|--------|---------|-------|-------|---------|
| 音量控制 | `loudness` | ✅ | ✅ | ✅ | `audio-controller` |
| 亮度控制 | `brightness` | ✅ | ✅ | ⚠️ | - |
| 壁纸设置 | `wallpaper` | ✅ | ✅ | ✅ | - |
| 窗口管理 | `@nut-tree/nut-js` | ✅ | ✅ | ✅ | `robotjs` |
| 应用启动 | `open` | ✅ | ✅ | ✅ | - |
| 进程列表 | `ps-list` | ✅ | ✅ | ✅ | - |
| 系统信息 | `systeminformation` | ✅ | ✅ | ✅ | - |
| 截图 | `screenshot-desktop` | ✅ | ✅ | ✅ | - |
| 浏览器控制 | `playwright` | ✅ | ✅ | ✅ | `puppeteer` |
| 键鼠模拟 | `robotjs` | ✅ | ✅ | ✅ | `@nut-tree/nut-js` |
| MCP 集成 | `@modelcontextprotocol/sdk` | ✅ | ✅ | ✅ | - |

**图例**: ✅ 完全支持 | ⚠️ 需要额外配置

### 2.1 备选方案说明

| 原方案 | 备选方案 | 选择建议 |
|--------|---------|---------|
| `loudness` | `audio-controller` | 备选更新更活跃，TypeScript 原生支持 |
| `robotjs` | `@nut-tree/nut-js` | nut.js 功能更全面，维护更好 |
| `playwright` | `puppeteer` | Playwright 支持更多浏览器 |

### 2.2 Linux 特殊依赖

| npm 包 | Linux 依赖 | 安装命令 (Ubuntu) |
|--------|-----------|------------------|
| `brightness` | brightnessctl | `sudo apt install brightnessctl` |
| `loudness` | pulseaudio-utils | `sudo apt install pulseaudio-utils` |
| `@nut-tree/nut-js` | libxtst-dev | `sudo apt install libxtst-dev libpng-dev` |
| `screenshot-desktop` | gnome-screenshot | `sudo apt install gnome-screenshot` |

> 详细 Linux 配置请参考 [10-Linux环境配置指南](./10-Linux环境配置指南.md)

---

## 三、系统命令映射

### 3.1 电源管理

| 功能 | Windows | macOS | Linux |
|------|---------|-------|-------|
| 锁屏 | `rundll32.exe user32.dll,LockWorkStation` | `pmset displaysleepnow` | `loginctl lock-session` |
| 休眠 | `rundll32.exe powrprof.dll,SetSuspendState` | `pmset sleepnow` | `systemctl suspend` |
| 关机 | `shutdown /s /t 0` | `osascript -e 'tell app "System Events" to shut down'` | `systemctl poweroff` |
| 重启 | `shutdown /r /t 0` | `osascript -e 'tell app "System Events" to restart'` | `systemctl reboot` |
| 定时关机 | `shutdown /s /t <seconds>` | `sudo shutdown -h +<minutes>` | `shutdown -h +<minutes>` |
| 取消关机 | `shutdown /a` | `sudo killall shutdown` | `shutdown -c` |

### 3.2 外观模式

| 功能 | Windows | macOS | Linux (GNOME) |
|------|---------|-------|---------------|
| 检查深色模式 | `reg query "HKCU\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize" /v AppsUseLightTheme` | `defaults read -g AppleInterfaceStyle` | `gsettings get org.gnome.desktop.interface color-scheme` |
| 设置深色模式 | `reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize" /v AppsUseLightTheme /t REG_DWORD /d 0 /f` | `osascript -e 'tell app "System Events" to tell appearance preferences to set dark mode to true'` | `gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'` |
| 设置浅色模式 | `reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize" /v AppsUseLightTheme /t REG_DWORD /d 1 /f` | `osascript -e 'tell app "System Events" to tell appearance preferences to set dark mode to false'` | `gsettings set org.gnome.desktop.interface color-scheme 'prefer-light'` |

### 3.3 应用管理

| 功能 | Windows | macOS | Linux |
|------|---------|-------|-------|
| 打开应用 | `start "" "AppName"` | `open -a "AppName"` | `AppName &` |
| 打开 URL | `start "" "https://..."` | `open "https://..."` | `xdg-open "https://..."` |
| 关闭进程 | `taskkill /PID <pid> /F` | `kill <pid>` | `kill <pid>` |
| 强制关闭 | `taskkill /IM "name*" /F` | `pkill -9 -f "name"` | `pkill -9 -f "name"` |

### 3.4 截图

| 功能 | Windows | macOS | Linux |
|------|---------|-------|-------|
| 全屏截图 | PowerShell 脚本 | `screencapture <path>` | `gnome-screenshot -f <path>` |
| 区域截图 | Snipping Tool | `screencapture -i <path>` | `gnome-screenshot -a -f <path>` |
| 窗口截图 | PowerShell 脚本 | `screencapture -w <path>` | `gnome-screenshot -w -f <path>` |

---

## 四、窗口管理快捷键

| 功能 | Windows | macOS | Linux (GNOME) |
|------|---------|-------|---------------|
| 左半屏 | `Win + Left` | `Ctrl + Option + Left` * | `Super + Left` |
| 右半屏 | `Win + Right` | `Ctrl + Option + Right` * | `Super + Right` |
| 最大化 | `Win + Up` | `Ctrl + Cmd + F` | `Super + Up` |
| 最小化 | `Win + Down` | `Cmd + M` | `Super + H` |
| 关闭窗口 | `Alt + F4` | `Cmd + W` | `Alt + F4` |

\* macOS 窗口平铺需要安装 Rectangle 等第三方工具

---

## 五、文件系统

| 功能 | Windows | macOS | Linux |
|------|---------|-------|-------|
| 用户目录 | `%USERPROFILE%` | `~` | `~` |
| 桌面 | `%USERPROFILE%\Desktop` | `~/Desktop` | `~/Desktop` |
| 文档 | `%USERPROFILE%\Documents` | `~/Documents` | `~/Documents` |
| 下载 | `%USERPROFILE%\Downloads` | `~/Downloads` | `~/Downloads` |
| 图片 | `%USERPROFILE%\Pictures` | `~/Pictures` | `~/Pictures` |
| 临时目录 | `%TEMP%` | `/tmp` | `/tmp` |
| 应用数据 | `%APPDATA%` | `~/Library/Application Support` | `~/.config` |

---

## 六、进程通信

| 方式 | Windows | macOS | Linux | Node.js 实现 |
|------|---------|-------|-------|-------------|
| 标准输入输出 | ✅ | ✅ | ✅ | `process.stdin/stdout` |
| WebSocket | ✅ | ✅ | ✅ | `ws` 包 |
| 命名管道 | `\\.\pipe\name` | `/tmp/name.sock` | `/tmp/name.sock` | `net` 模块 |
| HTTP | ✅ | ✅ | ✅ | `http` 模块 |

---

## 七、常用命令对照

| 操作 | Windows | macOS | Linux |
|------|---------|-------|-------|
| 打开文件 | `start "" "file"` | `open "file"` | `xdg-open "file"` |
| 获取系统版本 | `ver` | `sw_vers` | `lsb_release -a` |
| 获取主机名 | `hostname` | `hostname` | `hostname` |
| 获取用户 | `whoami` | `whoami` | `whoami` |
| 复制到剪贴板 | `clip` | `pbcopy` | `xclip` |
| 从剪贴板粘贴 | `powershell Get-Clipboard` | `pbpaste` | `xclip -o` |
| 查看端口 | `netstat -ano` | `lsof -i` | `ss -tulpn` |
| 环境变量 | `%VAR%` | `$VAR` | `$VAR` |

---

## 八、Node.js 跨平台工具函数

### 8.1 平台检测

```typescript
export function getPlatform(): 'darwin' | 'win32' | 'linux' {
  return process.platform as 'darwin' | 'win32' | 'linux';
}

export const isMacOS = () => process.platform === 'darwin';
export const isWindows = () => process.platform === 'win32';
export const isLinux = () => process.platform === 'linux';
```

### 8.2 平台选择器

```typescript
export function platformSelect<T>(options: {
  darwin?: T;
  win32?: T;
  linux?: T;
  default: T;
}): T {
  const platform = process.platform as 'darwin' | 'win32' | 'linux';
  return options[platform] ?? options.default;
}

// 使用示例
const lockCommand = platformSelect({
  darwin: 'pmset displaysleepnow',
  win32: 'rundll32.exe user32.dll,LockWorkStation',
  linux: 'loginctl lock-session',
  default: '',
});
```

### 8.3 跨平台命令执行

```typescript
import { exec } from 'child_process';
import { promisify } from 'util';

const execAsync = promisify(exec);

export async function runPlatformCommand(commands: {
  darwin?: string;
  win32?: string;
  linux?: string;
}): Promise<{ stdout: string; stderr: string }> {
  const platform = process.platform as 'darwin' | 'win32' | 'linux';
  const command = commands[platform];
  
  if (!command) {
    throw new Error(`Platform ${platform} not supported`);
  }
  
  return execAsync(command);
}
```

### 8.4 路径处理

```typescript
import * as path from 'path';
import * as os from 'os';

export function getHomePath(): string {
  return os.homedir();
}

export function getDesktopPath(): string {
  return path.join(os.homedir(), 'Desktop');
}

export function getDownloadsPath(): string {
  return path.join(os.homedir(), 'Downloads');
}

export function getAppDataPath(): string {
  const platform = process.platform;
  if (platform === 'win32') {
    return process.env.APPDATA || path.join(os.homedir(), 'AppData', 'Roaming');
  } else if (platform === 'darwin') {
    return path.join(os.homedir(), 'Library', 'Application Support');
  } else {
    return path.join(os.homedir(), '.config');
  }
}
```

---

## 九、依赖安装

### 9.1 npm 包安装

```bash
# 核心依赖
npm install loudness brightness wallpaper @nut-tree/nut-js open ps-list systeminformation screenshot-desktop playwright ws

# 开发依赖
npm install -D typescript @types/node vitest
```

### 9.2 平台特定依赖

| 平台 | 额外依赖 | 安装命令 |
|------|---------|---------|
| Linux | brightnessctl | `sudo apt install brightnessctl` |
| Linux | scrot | `sudo apt install scrot` |
| Linux | xclip | `sudo apt install xclip` |
| macOS | Rectangle (可选) | `brew install --cask rectangle` |

---

## 十、功能支持矩阵

| 功能 | Windows | macOS | Linux | 实现方式 |
|------|---------|-------|-------|---------|
| 音量控制 | ✅ | ✅ | ✅ | loudness |
| 亮度控制 | ✅ | ✅ | ⚠️ | brightness |
| 壁纸设置 | ✅ | ✅ | ✅ | wallpaper |
| 深色模式 | ✅ | ✅ | ✅ | 系统命令 |
| 锁屏 | ✅ | ✅ | ✅ | 系统命令 |
| 休眠 | ✅ | ✅ | ✅ | 系统命令 |
| 关机/重启 | ✅ | ✅ | ✅ | 系统命令 |
| 打开应用 | ✅ | ✅ | ✅ | open |
| 关闭应用 | ✅ | ✅ | ✅ | ps-list + kill |
| 截图 | ✅ | ✅ | ✅ | screenshot-desktop |
| 窗口管理 | ✅ | ⚠️ | ✅ | nut.js |
| 浏览器控制 | ✅ | ✅ | ✅ | playwright |
| 系统信息 | ✅ | ✅ | ✅ | systeminformation |
| 剪贴板 | ✅ | ✅ | ⚠️ | 系统命令 |

**图例**: ✅ 完全支持 | ⚠️ 部分支持/需要额外配置

---

**文档版本**: 2.0.0  
**维护者**: AIOS Protocol Team

---

## 版本变更记录

- 2026-01-25：补充状态/适用范围/维护人元信息，整理文档层级与索引结构。
