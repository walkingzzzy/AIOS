{
  "name": "aios",
  "version": "0.1.0",
  "private": true,
  "description": "AIOS Protocol - 跨平台 AI 系统控制协议",
  "scripts": {
    "dev": "pnpm --filter daemon dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.6.0",
    "tsup": "^8.0.2",
    "@types/node": "^20.11.0"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
