/**
 * Confirmation 模块导出
 */

export type {
    ConfirmationStatus,
    ConfirmationRequest,
    ConfirmationHandler,
    ConfirmationManagerConfig,
} from './types.js';

export {
    ConfirmationManager,
    confirmationManager,
} from './ConfirmationManager.js';
