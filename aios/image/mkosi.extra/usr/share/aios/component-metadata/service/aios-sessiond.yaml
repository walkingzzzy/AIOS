service_id: aios-sessiond
plane: control
role: session-memory-store
implementation_status: in_progress
maturity: partial_impl
current_phase: phase3
primary_language: rust
async_runtime: tokio
transport:
  primary: uds-jsonrpc
storage:
  primary: sqlite-wal
  secondary:
    - filesystem-objects
inputs:
  - session_events
  - task_events
outputs:
  - session_state
  - task_state
  - memory_refs
  - recovery_refs
  - portal_handles
dependencies:
  requires: []
  optional:
    - provider-registry
blockers:
  - no-cross-service-evidence-consumer
next_milestones:
  - expand-sessiond-inline-and-cross-service-tests
  - harden-cross-service-evidence-consumer
