service_id: aios-policyd
plane: trust
role: policy-engine
implementation_status: in_progress
maturity: partial_impl
current_phase: phase2
primary_language: rust
async_runtime: tokio
transport:
  primary: uds-jsonrpc
storage:
  primary: sqlite-or-state-dir
  secondary:
    - audit-store
inputs:
  - task_plan
  - capability_request
  - provider_resolution
outputs:
  - approval_decision
  - execution_token
  - policy_events
dependencies:
  requires:
    - aios-sessiond
  optional:
    - provider-registry
blockers:
  - no-formal-shell-approval-surface
next_milestones:
  - expand-policy-approval-tests
  - harden-shell-approval-surface-integration
  - extend-cross-service-policy-smoke
